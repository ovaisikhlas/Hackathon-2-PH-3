# OpenAI Agents SDK Integration Skill

## Description
Configure OpenAI Agents SDK runner, build message history from DB, invoke agent with MCP tools, capture tool calls and final response.

## Assigned to
Chatbot Backend Engineer Agent

## Usage
Running agent in /api/{user_id}/chat.